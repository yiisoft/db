on:
  pull_request:
    paths: &paths
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/db-sqlite.yml'
      - 'composer.json'
      - 'phpunit.xml.dist'

  push:
    branches: ['master']
    paths: *paths

name: db-sqlite

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    name: SQLite latest

    uses: yiisoft/actions/.github/workflows/db-sqlite.yml@ab62d6b3b0e0cff6c9724ec5a395bedb41c639a2
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
