on:
  pull_request:
    paths: &paths
      - 'src/**'
      - 'tests/**'
      - '.github/workflows/mutation.yml'
      - 'composer.json'
      - 'infection.json.dist'

  push:
    branches: ['master']
    paths: *paths

name: mutation

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    uses: yiisoft/actions/.github/workflows/db-mutation.yml@ab62d6b3b0e0cff6c9724ec5a395bedb41c639a2
