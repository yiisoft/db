<?php

declare(strict_types=1);

namespace Yiisoft\Db\Driver;

/**
 * The DriverInterface class is an interface which represents a database driver. A driver is a specific implementation
 * of a database connectivity layer, such as `PDO` or `MySQLi`. The interface defines the methods that must be
 * implemented by a concrete driver class, such as methods for connecting to a database, executing queries, and managing
 * transactions.
 */
interface DriverInterface
{
}
